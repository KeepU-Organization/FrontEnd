export interface ModulesResponse {
    id: string;
    title: string;
    description: string;
    orderIndex:number;
    courseCode:string
    imageUrl:string
    duration: number;
    code: string;
}